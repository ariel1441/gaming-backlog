# Unscheduled Ideas

Last updated: 2026-07-11

These are unverified ideas that are not active priorities. Completed ideas have
been removed. Prefer [`../ROADMAP.md`](../ROADMAP.md) when choosing work.

- Normalize personal genre values to avoid duplicates such as `Action` and
  `action`.
- Add CSV/JSON import and, later, console/manual library import.
- Improve cache invalidation after the audit's correctness issues are fixed.
- Use the shared chart-empty component consistently.
- Consider a dedicated TypeScript migration as a large standalone refactor.
