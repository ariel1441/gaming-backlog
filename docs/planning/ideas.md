# Ideas

Status: unverified planning notes. Check the current code before implementing
any item here. Prefer `docs/ROADMAP.md` for current priorities.

- Add a filter option by hours: make a range control where the user can change the minimum and maximum hours for a game. Done baseline.
- Add a quick filter for finished and "played a lot but didn't finish" games. Done baseline.
- Change the project to TypeScript. Difficulty: very hard.
- Add an option to import a Steam library, CSV file, or maybe a console library. Difficulty: medium/hard.
- Make the "my genre" selection support multiple selections again. Done baseline.
- Normalize "my genre" to lowercase to avoid duplicates like `Action` and `action`. Difficulty: easy.
- Improve cache usage and invalidation logic. Difficulty: medium. Read `metadata-catalog-refactor.md` first.
- Improve security through smaller hardening steps. Difficulty: medium.
- Use `ChartEmpty.jsx` where relevant. Difficulty: easy, low priority.

